# Only needed when running the frontend and backend as separate dev servers
# (npm run dev on :5173 talking to uvicorn on :8000). Leave unset in
# production -- the backend serves the built frontend from the same origin,
# so a same-origin (empty) API_BASE is correct there.
VITE_API_BASE=http://localhost:8000
